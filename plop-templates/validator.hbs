import Joi from 'joi';

/**
 * Joi validation schema for {{pascalCase lastFolder}} create/update operations.
  **/
export const {{pascalCase lastFolder}}Validator = {
  createOrUpdate: Joi.object({
    // Add your Joi validation fields here
  })
};
