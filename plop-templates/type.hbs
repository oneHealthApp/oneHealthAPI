import { Prisma } from '@prisma/client';

/**
 * Data types used across {{pascalCase lastFolder}} module.
 */

 /**
 * Input type for creating a new {{pascalCase lastFolder}}.
 * Mirrors Prisma.{{pascalCase lastFolder}}CreateInput.
 */
export type {{pascalCase lastFolder}}CreateInput = Prisma.{{pascalCase lastFolder}}CreateInput;

/**
 * Input type for updating an existing {{pascalCase lastFolder}}.
 * Mirrors Prisma.{{pascalCase lastFolder}}UpdateInput.
 */
export type {{pascalCase lastFolder}}UpdateInput = Prisma.{{pascalCase lastFolder}}UpdateInput;