/* This file is used to create a Prisma client instance that
 can be reused across the application. */

import { PrismaClient } from '@prisma/client';

const prisma = new PrismaClient();

export default prisma;
